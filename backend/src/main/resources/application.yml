spring:
  application:
    name: inclusoft-api

  profiles:
    default: local

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
